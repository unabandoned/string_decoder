name: release-cut
on:
  schedule:
    - cron: '0 6 1 * *'
  workflow_dispatch:
jobs:
  cut:
    permissions:
      contents: write
      pull-requests: write
    uses: unabandoned/.github/.github/workflows/reusable-release-cut.yml@ba99cd9a9fdc2dc58c550b1793a923e89e6bd4c9 # v1.2.0
    secrets: inherit
